<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

  <div class="w-full h-screen relative">

    <% if(errmessage2.length> 0){ %>
      <div class=" bg-red-500 border border-red-400 text-white w-52  px-4 py-3 rounded relative" role="alert">
        <span class="block sm:inline">
          <%= errmessage2 %>
        </span>
      </div>
      <% } %>

        <div>
          <h1 class="text-gray-600 font-bold text-xl font-sans px-4 pt-2 md:px-16">Scatch</h1>
        </div>
        <div
          class="absolute top-1/2 left-1/2  transform -translate-x-1/2 -translate-y-1/2  w-full lg:w-2/4 md:w-5/6 px-4">
          <h1 class="w-full text-4xl font-semibold">Login Your Account</h1>
          <p class=" font-semibold">Don't have account? <a class="text-blue-400" href="/">Register now</a></p>
          <h4 class="font-bold text-red-700">
            <%= errmessage %>
          </h4>
          <form action="/users/login" method="post" class="mt-3 flex flex-col gap-2">
            <input class="block w-full md:w-3/4 bg-gray-200 py-1 px-2 rounded outline-none" type="email"
              placeholder="Email" name="email">
            <input class="block w-full md:w-3/4 bg-gray-200 py-1 px-2 rounded outline-none" type="password"
              placeholder="Password" name="password">
            <input type="submit" value="Login" class="bg-blue-500 w-28  text-white rounded-full  py-1.5 mt-2">
          </form>
        </div>
  </div>
</body>

</html>